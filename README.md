# Spring Boot Heroku demo

commands to run
heroku login
heroku create
// check dynow with:
//// heroku ps
// if needed
//// heroku ps:scale web=1
// commit project
git commit heroku master
git open
git logs -t


by aber